name: proejct-ci

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          name: jar
          path: target/baidu-index-*.jar
          retention-days: 5
      - name: dependencies
        run: lein deps
      - name: test
        run: lein test
      - name: build-package
        run: lein uberjar