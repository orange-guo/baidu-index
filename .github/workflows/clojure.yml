name: proejct-ci

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: dependencies
        run: lein deps
      - name: test
        run: lein test
      - name: build-package
        run: lein uberjar